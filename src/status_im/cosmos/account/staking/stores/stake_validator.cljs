(ns status-im.cosmos.account.staking.stores.stake-validator)

(defn do-stake [validator-address])

;CLicking stake should open a popover

; It should have the following fields
;amount
;memo

;clicking next should call the following function